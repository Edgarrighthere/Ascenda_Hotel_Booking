<RangeSlider
                                    min={priceRange[0]}
                                    max={priceRange[1]}
                                    defaultValue={priceRange}
                                    onInput={handlePriceRangeChange}
                                    className="rangeSlider"
                                />